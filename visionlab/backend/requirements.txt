# ───────── Core Framework ─────────
fastapi==0.111.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# ───────── Async Database ─────────
sqlalchemy[asyncio]==2.0.30
asyncpg==0.29.0
alembic==1.13.1
psycopg2-binary==2.9.9

# ───────── Auth ─────────
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
pydantic[email]==2.8.2
pydantic-settings==2.4.0

# ───────── Task Queue ─────────
celery==5.4.0
redis==5.0.4
flower==2.0.1

# ───────── AI / ML ─────────
ultralytics==8.3.0
deepface==0.0.93
easyocr==1.7.1
transformers==4.41.0
Pillow==10.3.0
opencv-python-headless==4.9.0.80
numpy==1.26.4
accelerate==0.31.0
sentencepiece==0.2.0

# ───────── TensorFlow / DeepFace compatibility ─────────
# tf-keras major.minor must match tensorflow major.minor exactly.
tensorflow==2.16.2
tf-keras==2.16.0

# ───────── Gemini Story Generation ─────────
google-generativeai==0.8.6

# ───────── TTS ─────────
gTTS==2.5.1

# ───────── Translation ─────────
deep-translator==1.11.4

# ───────── Utilities ─────────
httpx==0.27.0
aiofiles==23.2.1
python-dotenv==1.0.1
structlog==24.1.0
pytest==8.2.2

# ───────── Evaluation (eval/ module — CER + emotion metrics) ─────────
# Pinned versions confirmed (via `pip install --dry-run` against this image)
# to be satisfied by the existing numpy==1.26.4 pin — do not let either of
# these pull in numpy>=2 on a future upgrade.
jiwer==3.0.4
scikit-learn==1.5.1
# Ultralytics' DetectionValidator auto-detects a COCO val2017-format dataset
# (eval/coco_val.yaml) and runs an extra pycocotools-based mAP cross-check
# unconditionally — without this pinned here, it silently pip-installs
# itself at eval time instead (works fine via a prebuilt Linux wheel, no
# build step, but non-deterministic and requires network access at eval
# time rather than at image build time).
pycocotools==2.0.11
